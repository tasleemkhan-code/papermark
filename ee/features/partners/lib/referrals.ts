   // Stub for the Enterprise Edition "Partners/Referrals" feature,
   // which isn't included in the open-source repo. Disables the
   // referrals nav link in the community edition.
   export function isReferralsEnabled(): boolean {
     return false;
   }
